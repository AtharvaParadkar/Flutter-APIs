import 'package:flutter/material.dart';

class PostApiCallScreen extends StatelessWidget {
  const PostApiCallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}