import 'package:flutter/material.dart';

class PostApiController extends StatelessWidget {
  const PostApiController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}